<!--
Generated by scripts/sync-repo.sh from github.com/mpacarroll/ai-instructions.
Do not edit the generated sections by hand; edit the sources in ai-instructions
(templates/CLAUDE.md.tpl and repos/mcp-timeline/PROFILE.md) and re-run the script.
The "Repo knowledge" section at the bottom belongs to this repo; add to it freely.
-->

# mcp-timeline

This repository uses the shared [ai-instructions](https://github.com/mpacarroll/ai-instructions) system.

- Stack: `python-mcp-server`, conventions at [stacks/python-mcp-server/CONVENTIONS.md](https://github.com/mpacarroll/ai-instructions/blob/main/stacks/python-mcp-server/CONVENTIONS.md)
- Repo profile: [repos/mcp-timeline/PROFILE.md](https://github.com/mpacarroll/ai-instructions/blob/main/repos/mcp-timeline/PROFILE.md)
- Durable rules: [global/](https://github.com/mpacarroll/ai-instructions/tree/main/global)

## Claude Code setup (automatic)

The checked-in `.claude/settings.json` pins the `mpacarroll-ai` plugin marketplace and enables the `carroll-core` plugin. On any machine (VS Code, desktop, terminal) and in cloud sessions, accepting workspace trust provisions:

- the core operating rules, injected into context at session start,
- all shared skills (`design-gate`, `coding-workflow`, `commit-push-pr`, `code-review`, `feature-implementation`, `docs-sync`, and the rest),
- all shared agents (`coding-subagent`, `pr-reviewer`, `art-director`, and the rest).

No per-machine setup is required. Other tools (Cursor, Perplexity, terminal agents) read `AGENTS.md` and the links above instead.

## Quick rules (for tools without the plugin)

The canonical source is [`global/`](https://github.com/mpacarroll/ai-instructions/tree/main/global).

- Move, do not stall. Two genuine attempts, then pivot.
- Never substitute demo data when a live action fails. Retry, then report failure.
- Never approve PRs. Open as draft. Never merge.
- No employer references in commits, PR descriptions, code, or generated docs.
- Tests and docs are part of the deliverable, not a follow-up.
- American English spelling. No em dashes. No exclamation points.

## Repo knowledge

Durable, repo-specific context lives here and in `.claude/rules/`. Architecture notes, decisions, gotchas, and anything a fresh session should know about this repo: add it below or as a file under `.claude/rules/` (use `paths:` frontmatter to scope a rule to part of the tree). This section is not overwritten by sync-repo.sh; re-runs preserve everything below the marker.

In-flight task state is separate: it lives in `.claude/state/handoff.md`, written by any session that stops mid-task (the injected Coordination rules make this automatic) and injected into the next session by the plugin's SessionStart hook. Durable facts here, current-task state there.

<!-- repo-knowledge: content below this line is preserved by sync-repo.sh -->
